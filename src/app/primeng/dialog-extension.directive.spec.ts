import { DialogExtensionDirective } from './dialog-extension.directive';

describe('DialogExtensionDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogExtensionDirective();
    expect(directive).toBeTruthy();
  });
});
